const { Password, Type } = require('./model')

!(async function () {

    
    Account.create(
        {
            account,
            password,
            mark,
            slug: slug ? slug : mark,
            remark: remark ? remark : '',
            typeId
        }

    )
})()